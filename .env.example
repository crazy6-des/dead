# Netlify frontend → Cloudflare Worker API
# Set this in Netlify's environment variables for deployed environments.
# Leave unset only when intentionally running the frontend without the API.
VITE_API_BASE_URL=
